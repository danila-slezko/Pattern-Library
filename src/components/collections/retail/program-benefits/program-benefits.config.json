{
  "status": "exported",
  "tags": ["marketing"],
  "preview": "@preview",
  "context": {
    "blockLayout": "imgRight",
    "clientName" : "{{ clientName }}",
    "clientSite" : "{{ clientSite }}"
  }
}

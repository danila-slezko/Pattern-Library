{
  "status": "exported",
  "tags": ["marketing", "landing"],
  "preview": "@preview",
  "context": {
    "blockLayout": "imgRight",
    "clientName" : "{{ clientName }}",
    "clientSite" : "{{ clientSite }}"
  }
}
